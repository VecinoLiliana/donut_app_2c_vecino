import 'package:flutter/material.dart';
class DonnutTab extends StatelessWidget {
  const DonnutTab({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DonnutTab'));
  }
}
